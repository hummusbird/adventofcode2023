namespace advent_16;

class Program
{
    static void Main(string[] args)
    {
        List<string> input = File.ReadAllLines("test.txt").ToList();
        List<string> board = input.ToList();



        for (int y = 0; y < board.Count; y++)
        {
            for (int x = 0; x < board[y].Length; x++)
            {

            }
        }
    }
}
